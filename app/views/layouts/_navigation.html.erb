<nav class="navbar navbar-expand-lg navbar-dark bg-dark static-top">
            <div class="container">
                <div class="logowebsite"><h2><%= link_to "DOANTODAY", root_path %></h2></div>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" data-toggle="collapse" id="navbarResponsive">
                    <ul class="navbar-nav ml-auto">
                        <li class="<%= 'nav-item active' if current_page?(root_path) %>">
                            <%= link_to root_path, class: "nav-link" do %>
                                Home <%= (content_tag :span, "(current)", :class => "sr-only") if current_page?(root_path) %>
                            <% end %>
                        </li>
                        <li class="<%= 'nav-item active' if current_page?(posts_path)%>">
                            <%= link_to posts_path, class: "nav-link" do %>
                            Posts <%= (content_tag :span, "(current)", :class => "sr-only") if current_page?(posts_path) %>
                            <% end %>
                        </li>
                        <li class="<%= 'nav-item active' if current_page?(photos_path)%>">
                            <%= link_to photos_path, class: "nav-link" do %>
                            Photos <%= (content_tag :span, "(current)", :class => "sr-only") if current_page?(photos_path) %>
                            <% end %>
                        </li>
                        <% if user_signed_in? %>
                        <li class="<%= 'nav-item active' if current_page?(user_path(current_user))%>">
                            <%= link_to user_path(current_user), class: "nav-link" do %>
                            Connected as:
                            <% if current_user.username %>
                            <%=  current_user.username %>
                            <% else %> 
                            <%= current_user.email %>
                            <% end %>
                            <%= (content_tag :span, "(current)", :class => "sr-only") if current_page?(user_path(current_user)) %>
                        <% end %> 
                        </li>
                        <li class="nav-item"><%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: "nav-link" %></li>
                        <% else %>
                        <li class="<%= 'nav-item active' if current_page?(new_user_session_path)%>">
                            <%= link_to new_user_session_path, class: "nav-link" do %>
                            Sign In <%= (content_tag :span, "(current)", :class => "sr-only") if current_page?(new_user_session_path) %>
                            <% end %>
                        </li>
                        <li class="<%= 'nav-item active' if current_page?(new_user_registration_path)%>">
                        <%= link_to new_user_registration_path, class: "nav-link" do %>
                            Sign Up <%= (content_tag :span, "(current)", :class => "sr-only") if current_page?(new_user_registration_path)%>
                        <% end %>
                        </li>
                        <% end %>
                        <li class="<%= 'nav-item active' if current_page?(about_path)%>">
                            <%= link_to about_path, class: "nav-link" do %>
                             About me <%= (content_tag :span, "(current)", :class => "sr-only") if current_page?(about_path) %>
                            <% end %>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>


